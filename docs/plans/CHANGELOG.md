# CHANGELOG

## v2.29.0: 2026-07-01
### 공개 유출 방지 (민감정보가 공개 레포에 새는 것 차단)
- 배경: "구조 노출로 정보 탈취?" 질문에서 출발. 구조 노출은 위험 아님(공개 레포는 원래 다 보임, security through obscurity는 약한 방어). 진짜 위험은 "내용에 비밀이 섞여 공개되는 것"
- 계열: v2.21(로컬≠커밋) 안전 버전. 그때는 "빠진 게 있나", 이번은 "새면 안 될 게 섞였나"
- guard.sh(2차망) 강화: git add/commit/push에 비밀 패턴(sk-·api_key·private key·AKIA·password=·xox 토큰) 있으면 경고+멈춤(exit 2). 오탐 가능해 "차단"보다 "사람이 확인"(진짜 비밀이면 빼고, 오탐이면 재실행). 4개 케이스 테스트 통과
- golden-set G14 추가(배포·커밋 유형): 공개 전 git diff --staged로 민감정보 확인, 비밀은 환경변수·.env로, 이미 커밋됐으면 키 폐기·재발급
- guardrails 보안: "공개 전 유출 점검" [MUST] 구체화
- 원칙: 숨겨서 지키기 아니라 "공개돼도 안전하게". guard.sh는 1차 경고일 뿐 사람이 최종 확인
- (보강) STACK-OPTIONS 전체 카테고리 인기 항목 보충(2026 채택도 검증): ORM(Prisma·Drizzle)·상태관리(Zustand)·서버상태(TanStack Query)·폼(React Hook Form)·린터(ESLint·Biome)·백엔드 대안·배포 대안. 신생 트렌드 제외, 널리 쓰임만


## v2.28.0: 2026-07-01
### STACK-OPTIONS.md 스택 선택 참고 카탈로그 (정보 제공용)
- 필요: 스택 고를 때 "어떤 선택지가 있나" 정보 제공. 잘 알려진 기술을 누구나 참고·사용해보게. 강요 아님
- 검토: "트렌드 자동 반영"은 거부(검증 안 된 신기술 = 예쁜 쓰레기 위험). 대신 "잘 알려지고 널리 쓰이는 것"(채택도=검증) 위주 + 의식적 갱신
- STACK-OPTIONS.md 신규: 웹서비스 스택 7개 카테고리(기반·도구·인프라·화면·결제·운영·확장), 각 항목 대표+대안
  · 수록 원칙 명시: "누락없이"=잘 알려진 후보를 빠짐없이(무명까지 다 아님). 널리 쓰임 필터가 유용성 유지
  · 한국 특화(포트원·카카오·Pretendard 등)는 🇰🇷 라벨 ─ 지역 다르면 해당 지역 것으로
  · "떠오르는" 섹션: 대세 전 기술 격리(본 목록 안정성 보호)
  · "최신 유지법" 섹션: 자동 아님. 손볼 때 웹서치+채택필터로 의식적 갱신, 갱신 로그
- 자동 로드 안 함(토큰 절약). spec-interview §8-stack에서 "막막하면 참고" 연결
- (보강) README '구성' 파일 트리 갱신: 그동안 누락됐던 STACK-OPTIONS·PREREQUISITES·PUBLISH·install.sh·INDEX.md·archive·PERMISSIONS.md 반영, 슬래시 명령 10개→12개 정정
- (보강) §8 에셋·리소스 섹션 추가: 3D모델(Kenney·Quaternius·KayKit·Synty·PolyHaven)·텍스처·애니메이션(Mixamo)·사운드·아이콘·이미지. '기술'과 '재료' 구분. 웹서치로 2026 CC0/무료 소스 검증
- 원칙 유지: 강요 아닌 정보 제공. 결정은 사람. v2.26 범용화와 충돌 안 함


## v2.27.0: 2026-06-30
### 인터뷰 §0 '동기' 축 (왜 만드나 → 강도 조율)
- 필요: 인터뷰가 "어떻게 만드나(build it right)"엔 강한데 "왜/무엇을 만드나(build the right thing)"의 첫 단추가 없었음. "이거 왜 만들어요?"가 빠져 있었음
- 검토: CFO/CMO 역할극으로 AI가 시장성을 "판단"하는 건 거부(근거 없는 환각, "검증 없으면 예쁜 쓰레기" 위반). AI는 판단 아닌 "질문"만
- spec-interview §0 동기 축 추가(빠른 깊이에도 포함, 선택형): 수익/업무효율/학습/취미/남의요청/기타
- §2-0 신규: 동기가 기존 축들의 강도를 조절(새 질문 안 늘림)
  · 수익→완성도 높게·성공지표=매출·오래살 코드·§11 강화 / 취미·학습→가볍게·버려질 코드·§11 생략
- [MUST] AI는 동기를 판단·평가 안 함(시장성은 만든 후 실측의 영역). 어떤 동기도 정당
- PRD §1에 동기 필드 추가. 진행 표시 총축수 11→12(0축부터)
- 무게: 새 축은 §0 하나. 나머지는 "동기가 기존 축 강도를 정한다"로 연결(질문 수 거의 안 늘림)


## v2.26.0: 2026-06-25
### 범용화 패스 (특정 스택 색 제거, 인터뷰로 채우는 구조)
- 문제 인식: v2.24 권한 기본값에 특정 사용자 스택(clasp·vercel·aws·docker·npm·python)이 박혀 있었음. v2.25는 "PC 3대" 같은 특정 환경 표현. 범용 템플릿 원칙(인프라 미정) 위반
- settings.json: 언어·스택 무관 보편 명령만 남김(읽기·조회·검사 + git 기본 + rm·sudo 차단). npm·python·clasp·vercel·aws 전부 제거
- PERMISSIONS.md: "스택 정해지면 동사 기준으로 추가"하는 가이드로 재작성. 스택별은 예시로만(그대로 박지 말 것 명시)
- spec-interview §8-env 추가: 인터뷰/사전 확인에서 스택·멀티환경이 정해지면 → 그 프로젝트의 settings.json·핸드오프에 반영하도록 연결 (템플릿엔 안 박되, 알게 되면 채움)
- memory-context: "PC 3대·자리·집·노트북" → "여러 작업 환경"으로 일반화
- 원칙 재확인: 방법론·가치관엔 개인화, 특정 기술·환경엔 거리. 알아서 채우는 건 인터뷰의 몫
- (보강) README·PREREQUISITES에 '환경별 사용' 추가: 템플릿은 macOS/Linux 기준, Windows는 WSL 또는 Git Bash 필수(없으면 guard.sh 안전망 미작동). 범용성의 OS 구멍을 문서로 메움
- (보강) memory-context §0에 '세션 길이 관리' 한 줄: 긴 세션에서 딴 길로 새는 것 방지 → /compact·/clear로 정리, 맥락은 파일이 잡음. 문서는 가볍게, 실제 예방은 '세션 안 끊고 무한정 이어가지 않기' 습관


## v2.25.0: 2026-06-25
### 멀티 디바이스 핸드오프 (PC 여러 대 오가며 이어 작업)
- 필요: PC 3대를 오가며 한 프로젝트를 이어감. commit/push↔pull로 바톤 넘기기
- 핵심: git은 "커밋된 것"만 옮긴다. 코드뿐 아니라 상태 파일(roadmap·PLAN·HANDOFF)도 커밋돼야 다음 PC가 이어받음
- memory-context §2-1 추가 (멀티 디바이스 워크플로우):
  · 떠나는 PC: 상태 파일 갱신 → 사용자가 commit+push → 로컬=원격 일치 확인 후 종료
  · 이어받는 PC: 사용자가 pull → 새 세션 → roadmap·HANDOFF 읽고 위치 복원(/resume) = 바톤 받기
  · 약한 고리 명시: push 빠뜨리면 안 넘어감, HANDOFF는 수동, 여러 PC 동시 작업 금지(형태 B 충돌)
- /handoff에 멀티 디바이스 안내 추가(상태파일도 push 대상, 커밋은 사용자가 직접)
- 부품은 이미 있었음(roadmap·handoff·resume·세션복원). 흐름을 한곳에 명시한 것


## v2.24.0: 2026-06-25
### 권한 기본값 설정 (매번 승인 피로 감소, 보수적)
- 발견: 작업 시킬 때마다 bash 명령 승인(엔터)을 요구해 옆에 붙어 지켜봐야 함. 안전한 ls·cat·test까지 매번 물음
- settings.json permissions 추가 (보수적 3단계):
  · allow(자동, 29개): 읽기·조회·검사·빌드·테스트만(ls·cat·grep·git status/diff/log·npm test/build/lint·verify·clasp pull)
  · ask(확인, 17개): 상태 바꾸는 것(add·commit·push·install·mv·cp·node·python·clasp push·docker). npm run*은 ask, build/test/lint만 allow(npm run deploy 함정 방지)
  · deny(차단, 17개): 비가역·배포·삭제(rm·sudo·--force·reset --hard·deploy·--prod·aws delete/terminate·prune·dd)
- 설계 원칙: 의심스러우면 allow 아닌 ask. 와일드카드는 좁게. guard.sh(PreToolUse)가 2차망
- .claude/PERMISSIONS.md: 분류 기준(동사로 판단)·조정법(좁게 시작→넓히기)·금지(--dangerously-skip-permissions)
- harness-design에 "권한=1차 게이트, guard.sh=2차망" 연결
- 안전성: allow는 상태 안 바꾸는 것만, 위험은 deny+guard.sh 이중. "좁게 시작" 권고


## v2.23.0: 2026-06-25
### PLAN 인덱스 + 완료 아카이브 (문서 100개+ 관리)
- 필요: PLAN이 100~200개로 늘면 한 폴더에 섞여 "뭐가 끝났고 뭐가 진행중인지" 확인 불가
- 검토: 폴더로 상태별 분류(draft/in-progress/done)는 함정 → 상태 바뀔 때마다 파일 이동 → git 추적 끊김·링크 깨짐·AI 빠뜨림. 그래서 C+A 절충 채택
- INDEX.md 신규: 전체 PLAN을 한 장의 표로(상태·생성·최종수정·한줄). 200개도 한눈에, 파일은 안 옮김
  · 상태값 정의 포함: Draft / In Progress / Blocked / Done (검증판정 Pass/Hold/Retry, 미정 [?]와 구분)
- archive/ 신규: 완료(Done)되고 참조 끝난 PLAN만 이동. 완료분은 더 안 바뀌어 경로 변경에도 링크 안전. 진행중은 안 옮김
- 연결: memory-context(파일맵+세션시작 INDEX 우선 훑기), doc-sync(PLAN 추가·상태변경 시 INDEX 갱신, 완료 시 archive 이동)
- 균형: 진행중 문서는 평평하게 유지(git/링크 보존), 끝난 것만 치움. 한눈 관리는 INDEX가 담당


## v2.22.0: 2026-06-25
### PLAN/PRD 생성·수정 날짜 추적 (파일 안 메타 헤더 방식)
- 필요: 문서를 언제 만들고 언제 고쳤는지 확인이 어려웠음
- 검토 후 결정: 날짜를 "파일명"이 아니라 "파일 안 헤더"에 (파일명에 수정일 넣으면 git이 다른 파일로 인식→히스토리 끊김, 참조 링크 깨짐, 충돌)
- prd.template.md 헤더에 메타 추가: 생성일(고정) · 최종 수정일(갱신) · 상태
- agent-loop ⑤보고 동기화 점검에 "PLAN/PRD 수정 시 최종 수정일 갱신" [MUST] 연결
- doc-sync 매핑에 "PLAN/PRD 수정 → 최종 수정일 갱신" 행 추가 (AI가 빠뜨리면 잡히게)
- 생성일은 한 번만, 파일명·git 히스토리·링크는 보존


## v2.21.0: 2026-06-24
### 배포 함정 '로컬 != 커밋' 점검 (실배포 오류에서 발견)
- 발견: 커밋된 page.tsx가 import하는 LEARNING_PATH가 topics.ts의 미커밋 변경분에만 있어, 로컬 빌드는 통과(워킹트리 포함)했지만 Vercel은 커밋된 main으로 빌드해 실패
- 원인: 우리 검증(verify.sh·골든셋)은 "내가 보는 워킹트리"를 검사. 버그는 "워킹트리와 커밋의 차이"에 숨어서 검증 범위 밖이었음. (컨텍스트 길이 문제 아님)
- 계열: v2.17(코드 간 불일치)·v2.18(데이터 불일치)과 같은 "내 환경≠실제 환경" 문제. 가장 흔한 'git 환경 불일치'만 빠져 있었음
- 골든셋 G13 추가 (배포·커밋 작업일 때): 커밋 전 git status로 의존성 스테이징 확인. "로컬에서 됨 != 커밋된 상태에서 됨"
- 라우팅 표에 "배포·커밋" 유형 추가
- agent-loop ④검증에 [MUST] 커밋·배포 전 git status 추가
- G13 보강: `git show HEAD:<파일> | grep <심볼>`로 커밋된 결과물에 심볼이 실제 있는지 직접 확인(스테이징 확인보다 한 단계 확실). 실사용 AI가 독립적으로 같은 결론에 도달 = 외부 수렴 확인
- 가볍게: git status 한 줄 습관이면 막힘. 거대한 CI 시스템 안 만듦


## v2.20.0: 2026-06-24
### 멀티에이전트 두 형태 구분 + 여러 세션 동시 작업 충돌 방지 (실사용 발견)
- 발견: 한 프로젝트에서 세션 2개를 열어 각각 다른 대상(1번/2번 교육 프로그램)을 동시 수정했더니 서로의 산출물을 지움. 경로를 나눠도 "정리 본능"과 공유 파일 덮어쓰기로 충돌
- 원인 분석: 멀티에이전트에 두 형태가 있는데 템플릿은 형태 A(한 세션 오케스트레이션)만 다뤘음
  · 형태 A: 한 지휘자가 서브에이전트 거느림 → 지휘자가 조율 가능
  · 형태 B: 여러 세션 각자 AI → 지휘자 없음, 서로의 존재 모름 → AI 규칙으로 못 막음
- multi-agent.md 보강:
  · §0-1 두 형태 구분 표 + 형태 B 핵심 경고([MUST]): 같은 폴더 공유 시 덮어쓰기, AI 지시로 못 막음
  · §2 "담당 경로 밖은 무시"[MUST] 추가: 보지도·정리하지도 마(정리 본능이 남의 작업 지움)
  · §2-1 작업 공간 분리 추가: 순차(권장)·git 브랜치·폴더 복제. 같은 폴더 동시 수정 금지[MUST]
- agent-loop 동시성에 [MUST] 여러 세션 동시 수정 금지 연결
- 균형: 거대한 오케스트레이션 시스템은 안 만듦(과함). 형태 구분 + 무시 규칙 + 작업공간 분리 안내만


## v2.19.0: 2026-06-24
### 완료 계약(Goal Contract) 강화 (외부 패턴 수렴 확인 후 1개만 반영)
- 배경: 코딩 에이전트 도구들(Codex Goals, LazyCodex, Gajae-Code)이 독립적으로 같은 패턴(goal+evidence+loop)으로 수렴. 우리 방향과 일치 확인 ─ 또 한 번 외부 검증
- 발견: 우리는 "성공 기준(done)"은 묻는데 "막힘/중단 조건(blocked)"이 약했음. 긴 세션·자동 루프가 끝없이 돌거나 "된 것 같다"로 애매하게 닫힐 수 있었음
- PRD §5를 완료 계약 4요소로 강화:
  · ① 완료 상태(done) ② 검증 증거(evidence) ③ 제약(constraints) ④ 막힘·중단 조건(blocked/uncertain)
  · ④가 핵심: 멈춤 신호 + 멈출 때 남길 것(시도경로·증거·blocker·다음 입력) + uncertain은 [?]로
- agent-loop ④검증에 [MUST] 막힘 조건에서 멈춤 추가(끝없이 돌지 않게)
- 추종 안 함: tmux 병렬 worker·worktree·특정 도구 명령어는 체급·인프라 달라 제외
- 무게: 새 문서 0개. PRD 성공기준을 계약 형태로 조임 + 루프에 한 줄. 텐션 강화


## v2.18.0: 2026-06-23
### AI·외부 생성 데이터 검증 (실배포 오류에서 발견)
- 발견: AI가 생성한 JSON의 일부 필드가 가끔 누락된 채 와서 렌더링 단계에서 런타임 오류. 빌드·개발검증은 통과("개발 때 잘 나옴"). AI 출력은 매번 달라 가끔 빠지는 케이스가 배포 후 터짐
- 골든셋 G11c-G11d 추가 (AI·외부 생성 데이터 쓰는 작업일 때만):
  · G11c 출력 신뢰 금지: AI·외부가 준 데이터를 그대로 믿지 말고 받는 즉시 구조(필수 필드) 검증 관문
  · G11d 불완전 출력 대비: 누락·빈 값 와도 안전 처리(재요청·기본값·명확한 에러)
  · 검증 라우팅 표에 "AI·외부 생성 데이터 사용" 유형 추가
- 명시: 근본 해결은 도구(받는 지점 스키마 검증). 문항은 "AI 출력은 믿을 만하다"는 착각을 막는 안전장치
- 구체 구현(특정 검증 라이브러리)은 템플릿에 안 박음(인프라 미정). 관점만 보편 원칙으로


## v2.17.0: 2026-06-23
### 컴포넌트 연결 검증 + em-dash 사전 방지 (실사용 발견)
- 발견 1: 컴포넌트 간 참조 불일치(A 출력 != B 입력)가 빌드는 통과하고 배포에서 터짐. 가끔이지만 실서비스 영향 큼
  · 골든셋 G11a-G11b 추가: 연결 지점 검증(이름·형태 일치) + "빌드 통과 != 연결 정상"(동적언어는 실행/타입체크/통합테스트로 확인)
  · 검증 라우팅 표에 "컴포넌트 연결" 유형 추가
  · 명시: 근본 해결은 도구(TypeScript·통합테스트를 verify.sh에). 문항은 맹점 의식용 안전장치
- 발견 2: em-dash 방지를 매번 sed 사후청소만 했지 규칙이 문서에 없었음
  · guardrails "산출물 표기" 추가: em-dash(:) 사전 금지 [MUST] + AI 글 패턴 회피. 모든 글·UI·주석·커밋에 적용
  · 사후 청소가 아니라 사전 방지로 전환


## v2.16.0: 2026-06-22
### 외부 리뷰 의견 선별 반영 (옥석 가려 3개만 채택)
- 받은 의견 5개 중 검토 후 채택:
  · guard.sh 인터셉터 메시지 강화: 위험 명령 차단 시 "무엇을·왜 설명→승인→진행" 절차를 명확히 안내 (HITL 강제 가시화)
  · recipes verify 주입 가이드(§8-verify): 스택 정해지면 recipes에서 verify.sh 시작점 1회 주입. 단 동적 자동수정 금지(검증 기준은 사람 통제)
  · 로드맵 복구 최우선화: 컨텍스트 스위칭 잦은 1인 작업 위해 세션 시작 시 ROADMAP "현재 위치" 싱크를 최우선으로 명시
- 거절한 의견 (반영 안 함):
  · multi-agent·escalation 삭제 제안 → 거절. "1인=불필요"는 오해(멀티에이전트=AI 여럿, 에스컬레이션=AI가 막히면 사람에게. 1인일수록 필요)
  · intent-routing+decision-support 통합 제안 → 보류. 입구(의도 연결)와 과정(결정 보조)은 트리거가 달라 합치면 흐려짐. 문서는 개수가 아니라 명료함이 기준


## v2.15.0: 2026-06-22
### UI/UX 감성·차별화 + 입문자 스택 추천 (UI 서비스 조건부)
- 발견: 작동하는 UI는 묻는데 "갖고 싶은 UI"는 안 물었다. 같은 기능도 감성이 흥행을 가른다(카카오 캐릭터 카드 예). 동시에 입문자는 "뭘로 만들지" 스택 결정에서 벽을 만난다.
- 인터뷰 8축 UX 확장 (UI 서비스일 때만):
  · §8-design 감성·디자인: 첫인상에서 느낄 것, 경쟁자와 시각 차별점, 타겟 분위기
  · §8-stack 구현 스택 결정: 무엇으로 만들지 정하되 특정 스택 강제 안 함(인프라 미정)
- 입문자 스택 가이드: "모르겠다"면 용도별 기본값 추천(웹→React+Tailwind+shadcn 등). 추천일 뿐 강제 아님, 템플릿에 안 박음
- 골든셋 G7a: 감성·첫인상 검증("갖고 싶은가/경쟁자와 구별되는가"). 내부 도구·자동화 면제
- PRD 6-3에 감성·구현스택 칸 추가
- 균형: 전부 "UI 서비스일 때만" 조건부. 자동화·내부도구엔 0. 모르는 사람엔 추천, 아는 사람엔 비킴


## v2.14.0: 2026-06-21
### 자연어 의도 연결 (명령어를 몰라도 되게)
- docs/ops/intent-routing.md 신규: 사용자가 평소 말로 하면 맞는 흐름으로 연결
  · 의도→흐름 매핑 ("버그 고치고 싶어"→bugfix 흐름). 명령어 강요 안 함
  · 안전장치(핵심): 잘못 이해 위험을 위험도로 나눔
    - 낮은 위험: 이해한 대로 실행 + 보고
    - 모호: 추측 금지, 되묻기
    - 비가역(삭제·배포·되돌리기): "이해한 것 되말하고 확인" 후 실행
  · Echo-back: 실행 전 이해한 바를 사람 말로 되비춰 잘못된 '이해'에서 멈추게
  · 능동 안내: 분기점에서 다음 할 일을 사람 말로 먼저 제안
  · "뭐 할 수 있어?"엔 명령어 목록 대신 상황으로 답
- CLAUDE.md: 자연어 우선 규칙 + import. agent-loop ②결정(되묻기·비가역 확인) 위에 얹음
- 균형: 자연어 편의 + 안전. "다 알아서 해"가 아니라 "편하게 받되 모호·위험하면 멈춤"


## v2.13.1: 2026-06-21
### 전 문서 텐션 점검 (요청: 문서 자체가 텐션 떨어지지 않게)
- 13개 ops 문서를 텐션(강제력) 관점에서 전수 점검
- memory-context.md 텐션 보강 (가장 약했음 - 노트북 이동 연속성의 핵심인데 권고문 수준이었음):
  · 세션 시작 체크리스트를 [MUST]로: "맥락 복원 없이 작업 시작 금지" + 건너뛰면 결과(중복 결정·작업) 명시
  · "기억=파일" 원칙을 [MUST]로: 파일에 안 남긴 건 다음 세션에 없는 것
  · 침묵 스킵 금지: 복원 이력 없으면 "없음" 명시
- retro.md 임계치를 [MUST]로: 카운트 3 도달 시 격상 강제(그냥 카운트만 금지)
- 나머지 11개 문서는 텐션 충분 확인 (guardrails·doc-sync·decision-support·apply-loop 등은 명령형·금지 조항이 측정 숫자보다 강함)


## v2.13.0: 2026-06-21
### 검증 라우팅 + 회고 + 파일명 유래 (gstack 자극, 우리 철학으로 흡수)
- 검증 라우팅 (골든셋 텐션 강화): 작업 유형 판정을 의무화 → 해당 유형 문항을 빠짐없이 평가
  · "맞는 검증만" 때문에 느슨해지지 않게 "반드시"로 조임. 판정 유형을 결과표 맨 위에 명시, 침묵 스킵 금지
  · /validate 명령에 유형 판정 단계 추가
- 회고 docs/ops/retro.md + /retro 신규: 반복 마찰을 RETRO.md에 누적 카운트
  · 카운트 3+ → 구조적 문제로 격상(BACKLOG/v3 신호). 임계치가 텐션
  · 로드맵 단계 완료 시 회고 제안 연결. "회고가 곧 버전 진화의 엔진"
- 전 ops 파일에 "파일명 유래" 추가: 왜 이 이름인지 (11개 파일)
- gstack(YC 대표 도구)에서 검증 라우팅·회고 아이디어만 흡수. 23역할·브라우저자동화·인프라는 의도적 제외(규모·철학 다름)
- 균형: 무게가 아니라 텐션을 더함. retro.md는 얄팍한 양식이 아니라 반복을 카운트하고 임계치에 행동을 강제하는 밀도 있는 문서


## v2.12.0: 2026-06-21
### 결정·기획 보조 (실전 피드백 반영)
- 실전 진단: 인터뷰·실행·검수는 잘되나 "검수 후 결정·기획"이 어렵다 → 사람 몫을 돕는 레이어
- docs/ops/decision-support.md 신규: AI가 결정을 떠넘기지 않고 "고르기 쉽게 판 깔기"
  · 기획 보조: 꼭 정할 것 3개로 범위 좁히기 + 레퍼런스 패턴 + 지금/나중 분리
  · 결정 보조: 선택지에 트레이드오프·가역성·추천(이유) 필수. "알아서 고르세요" 금지
  · 검수 후 보조: 결과 보고 끝에 "그래서 다음 결정은 이것"까지 연결
- 연결: agent-loop ②결정·⑤보고, spec-interview(막힐 때), CLAUDE.md import
- 기존 [?] 미정 처리(roadmap §3)와 직결 - 못 정하면 안전하게 보류
- 무게: 새 게이트 0개, 문서 1개 + 연결 줄. HITL 유지(추천하되 결정은 사람)


## v2.11.0: 2026-06-21
### 로드맵 안전장치 (opt-in, 큰 프로젝트)
- docs/ops/roadmap.md 신규: 길 잃지 않고·끊겨도 잇고·몰라도 굴러가게
- 계층: ROADMAP(부모) / PLAN(자식) + 상태 롤업 (자식 Done → 부모 [x], 검증 통과로만)
- /resume 명령: 멈췄다 돌아오면 현재 위치·다음 진입점·미정 목록 브리핑
- 이탈 감지: 로드맵 벗어나면 복귀/수정/곁가지를 사람에게 물음 (AI 임의 변경 금지)
- [?] 미정 3종 정의: 보류(결정 미룸)·무지(정보 필요)·불확실(검증 필요) + 끝내 못 정하면 가역적 기본값
- 기능 추가 반영 규칙: 작은 건 PLAN만, 방향 바꾸면 로드맵+PLAN, 애매하면 질문
- 연결: CLAUDE.md import, 세션 체크리스트, agent-loop 정렬, PLAN 템플릿(부모 단계 줄)
- 무게/텐션 균형: opt-in(작은 작업은 PLAN만), 진행률 % 안 씀(셀 수 있는 사실만), 이탈 시 멈춰 묻는 텐션 유지


## v2.10.0: 2026-06-21
### 상업적 리스크 흡수 + 입문자 배려
- 리스크 흡수 (기존 축에 한 줄씩, 새 축 없음):
  · 7축 제약·위험: "놓치면 죽는 리스크"(비용 폭주·법규·보안) 한 줄
  · 8축 UX: 전환·이탈 마찰 지점 한 줄
  · 9축 운영: 비상 정지(kill-switch)·비용 상한·요청 제한 한 줄
  · PRD §7: 생존 안전장치 칸 + 상업 리스크 예시
- 입문자 배려:
  · 전문 용어 괄호 풀이 원칙
  · 어려운 질문엔 "모르면 '모름'" 안내 (막힘 방지)
  · "처음이에요" 신호 감지 시 자동 경량화(깊이↓·더 쉽게)
  · 깊이 선택 화면에 입문자 안내 추가
- 무게 관리: 새 축·새 파일 0개. InstaMind 문서의 범용 관점만 흡수, 특정 스택은 제외
- 10축→11축 표기 정합


## v2.9.0: 2026-06-21
### 역할별 기능 분리 + 사용자 여정 (다역할 서비스 지원)
- 인터뷰 4축(사용자·맥락)에 역할 파악 + 역할별 여정(Step) 캐묻기 추가 (역할 2개 이상일 때)
- 화면 구체성 가이드(§4-screen): 화면당 보는 것/할 수 있는 행동/결과 + 빈 상태·에러
- PRD 템플릿 §2-1 "역할별 기능/사용자 여정" 섹션 (opt-in, 역할 2개 이상일 때만)
  · 역할별 핵심 목적 + Step 표(화면/보는 것/행동/결과)
- 골든셋 G6a~G6c: 역할 누락·여정 완결·화면 구체성 검증 (다역할 서비스)
- init-project·spec 명령에 역할별 캐묻기 반영
- 역할 구성은 프로젝트마다 다름 존중 (고객+관리자 / 관리자+매니저 / 셋 다 등). 단일 역할이면 전부 건너뜀(YAGNI)


## v2.8.3: 2026-06-21
### 설치 안내 간소화
- README 방법1·2에서 'cd my-project' 줄 제거 (IDE에서 폴더 열고 터미널 켜면 이미 그 폴더라 불필요)
- 설치 명령을 degit/curl 한 줄로 단순화


## v2.8.2: 2026-06-21
### 공개용 변경 내역 정비
- 루트 CHANGELOG.md 신규: 버전별 마일스톤 요약 (Keep a Changelog 형식)
- README에 "버전 히스토리" 섹션 추가 → 사람들이 무엇이 바뀌었는지 파악
- doc-sync에 "버전 릴리스 시 3곳 함께 갱신" 규칙 추가


## v2.8.1: 2026-06-21
### 선택형 질문 기본화
- 인터뷰 모든 질문을 선택형(AskUserQuestion)으로 제시하는 것을 기본으로 강화
- 항상 마지막 보기로 "직접 입력" 제공 → 보기로 못 가두면서도 고르기 쉽게
- 보기 만드는 법 명시: 흔한 답 2~4개 구체 제시 (막연한 예/아니오 금지)
- 진짜 목표·가치 축도 보기 + "직접 입력" 강조로 자기 말 유도
- init-project·spec 명령에 동일 반영


## v2.8.0: 2026-06-21
### 인터뷰 끊김·조기개발 방지 (중대 수정)
- **조기 개발 금지 강제**: 고른 깊이의 모든 축을 다 묻고 사용자 확인 전엔 코드 생성 금지 (중간에 "충분하다"며 개발로 넘어가던 문제 해결)
- **진행 표시 [N/총축수]**: 매 질문에 현재 위치·남은 축 표시 → 사용자가 흐름 파악
- **되돌리기 허용**: "앞 질문 다시/N축으로 돌아가자" 즉시 반영 (일방통행 아님)
- **각 답변 후 "지금까지 정리" 갱신**: 흐름 놓침·오답 즉시 교정
- **보기·번호 선택형**: 가능한 질문은 번호 보기로 제시 (단 진짜목표·가치 축은 열린 질문 유지)
- 종료 조건에 "모든 축 완료 + 사용자 확인" 명시, 자가 점검 추가
- init-project·spec 명령, CLAUDE.md 진입점에 동일 규칙 반영


## v2.7.1: 2026-06-21
### 설치 안내 명확화
- README 방법1·2를 "이미 만든 프로젝트 폴더 안에서 루트에 바로 풀기"로 수정
- mkdir 중첩 패턴을 기본에서 빼고, 새 폴더 생성은 보조 안내로 이동
- 마지막 `.`의 의미("지금 이 폴더에 바로") 명시 → 하위 폴더 중첩 혼란 제거
- (install.sh는 TARGET 기본값이 이미 현재 폴더라 수정 불필요)


## v2.7.0: 2026-06-21
### 서늘한 질문 + 가치·검증 축
- 인터뷰 ★1 진짜 목표에 "존재 이유를 흔드는 질문" 추가 (누가 필요로 하나 / 없으면 곤란한가 / 왜 기존 걸 안 쓰나 / 안 만들면 무슨 일)
- 11번 축 "가치·검증" 신규 (표준+ 권장): 누가 왜 값 치르나 / 가장 싼 검증 / 핵심 가치에 닿나
- 인터뷰 규율에 "서늘하되 적대적이지 않게" 추가: 심문이 아니라 명료화, 방어적 답 방지
- PRD 템플릿 §6-5 가치·검증 칸 추가
- 10축 → 11축 표기 정합


## v2.6.0: 2026-06-21
### 표기 정리 + 사전 준비물
- 전체 파일에서 em dash(긴 줄표) 제거: 콜론(:)·괄호로 치환 (AI 글 특유 기호 정리, 44개 파일)
- PREREQUISITES.md 신규: 가져오기=Node.js만 / 개발=Claude Code·Python3·Git·스택도구
- README 사전 준비물 섹션에 PREREQUISITES.md 참조 추가
- 치환 후 전 게이트 작동 확인 (spec·golden·escalation·CI YAML 정상)


## v2.5.1: 2026-06-21
### 계정 반영
- 모든 <USER> 플레이스홀더를 only-victory로 치환 (README·install.sh·PUBLISH.md 12곳)
- 설치 명령이 별도 수정 없이 바로 작동: npx degit only-victory/agent-project-template .


## v2.5.0: 2026-06-21
### 공개 창고 모델 (가져다 쓰기)
- install.sh 추가: 공개 레포에서 파일만(.git 없이) 현재 폴더에 설치, degit 우선·git clone 폴백
- README 빠른시작 → "창고에서 가져오기" 4방법(degit/install.sh/Claude에게/기존레포 얹기)
- PUBLISH.md → "창고 1회 생성 + 이후 degit 한 줄" 흐름으로 재작성
- 핵심 모델: 원본 창고 1개 유지 → 새 프로젝트마다 degit으로 파일만 pull (창고와 git 분리)


## v2.4.0: 2026-06-21
### 공개 배포 준비
- README 전면 재작성: 설치법 3종(Template/clone/기존레포) + 명령어표 + 구성 + 정직한 한계
- .gitignore 추가: 민감정보(.env·.clasp.json·키) 커밋 방지
- PUBLISH.md 추가: 깃허브 최초 공개 절차 + Template repository 설정 안내
- 공개 전 민감정보 스캔 통과 (회사·실명·키 없음 확인)


## v2.3.1: 2026-06-21
### 스택 중립성 보정
- verify.sh 기본 주석을 스택 중립으로 (특정 스택을 디폴트처럼 보이지 않게)
- recipes/README.md에 '여러 스택 혼용' 섹션 추가 (Node+GAS 등 멀티스택 레포 가이드)
- 어느 스택도 대표가 아님을 명시: 레포가 실제 쓰는 것만 넣고 나머지 삭제


## v2.3.0: 2026-06-21
### 코드 품질 + 운영 사이클 (정직한 결론: A1+B1+B2)
- **A1 verify.sh 레시피 팩**: docs/ops/kit/recipes/ (웹·Python·GAS·정적웹 4종)
  · 버전 박제 없이 "패턴"만, "실제 명령으로 교체" 명시 → 신선도 위험 회피
  · GAS는 clasp 특수성 반영(로컬 실행 불가 → 정적 점검까지만 + HITL)
- **B1 운영 런북 (opt-in)**: docs/ops/templates/ops-runbook.template.md (씨앗)
  · /ops-init 으로 활성화, /incident 로 장애 대응 진입
  · 안 켜면 강제 0 (확장 제한 없음). 최종 검증일로 신선도 관리
- **B2 버그→재발방지 환류**: 골든셋 G12 (표준+ 권장, 빠른 깊이·오타성·프로토타입 면제)
  · 새 문서 없이 기존 골든셋에 질문 1개 (중복 회피). bugfix 명령에 마무리 단계로 연결
  · BACKLOG 1번(자동 테스트 도입)과 비충돌: 인프라 없으면 "도입 신호"로만
### 갇힘 방지 (사용자 우려 반영)
- 모든 신규 요소에 깊이 면제 또는 opt-in/opt-out 탈출구 내장
- 강제 게이트는 0개 추가: A1=재료, B1=opt-in, B2=권장


## v2.2.0: 2026-06-21
### 인터뷰 확장: 완성도 지향
- `spec-interview.md` 6축 → **10축** (데이터·상태, 엣지·실패, UX 흐름, 운영·관측 추가)
- **깊이 선택** 도입: 빠른(3축)/표준(8축)/깊게(10축): 매 인터뷰 시작 시 선택
- **이중 강제**: 체크리스트(필수 3축 게이트) + 완성도 점수(충실/부분/미답 → %)
- `prd.template.md` §6-1~6-4 신규 카테고리 칸 + §8-1 인터뷰 완료 리포트 칸 추가
- `spec-gate.sh` 확장: In Progress PLAN에 인터뷰 리포트 없으면 fail (인터뷰 거쳤다는 증거 강제)
- `spec.md`/`init-project.md` 슬래시 명령: 깊이 선택 + 10축 흐름 반영
- 고위험 신호(돈·인증·삭제·외부전송) 감지 시 깊이 상향 1회 제안 규칙


## v2.1.0: 2026-06-20
### 강제(Enforcement): 권고를 게이트로
- `docs/ops/kit/golden-gate.sh` 신규: Done PLAN에 골든셋 결과 없으면 fail (의미 검증 강제)
- `docs/ops/kit/escalation-gate.sh` 신규: 에스컬레이션 경로 미설정 시 CI 차단 ([SINGLE-AGENT] 마커로 면제)
- `verify.sh`: spec-gate + golden-gate를 단일 진입점에 통합
- `.github/workflows/verify.yml`: golden-gate·escalation-gate 스텝 추가
- `prd.template.md` §9 골든셋 검증 결과 칸 추가 (Done 전 필수)
### 검증
- 빈 템플릿 / Done(골든셋 무) / Done(골든셋 유) / In Progress / 에스컬레이션 빈칸·마커: 6개 시나리오 통과 확인
- 에스컬레이션 검사의 fail-open 버그(설명문 마커 오인식) 수정 → '담당:' 줄 앵커링

## v2.0.0: 2026-06-18
### 추가
- `docs/ops/multi-agent.md`: 역할 분리(기획/실행/검토), 퀄리티 게이트 3단계(통과/보류/재시도), HITL 트리거, 에스컬레이션 경로
- `docs/ops/golden-set.md`: 의미 검증 질문 세트 (G1~G11), `/validate` 명령 기반
- `docs/ops/memory-context.md`: 세션 간 컨텍스트 연속성, HANDOFF.md 패턴, ADR 재사용
- `.claude/commands/validate.md`: 골든셋 의미 검증 슬래시 명령
- `.claude/commands/escalate.md`: 작업 중단 + 에스컬레이션 슬래시 명령
- `.claude/commands/handoff.md`: 세션 인수인계 문서 생성 슬래시 명령
- `guardrails.md`에 HITL·퀄리티 게이트·에스컬레이션 섹션 추가

### 변경
- `CLAUDE.md`: multi-agent.md·memory-context.md import 추가, §2 검증에 의미 검증(/validate) 추가, §6 멀티에이전트 섹션 추가
- `GETTING-STARTED.md`: §7~§10 신규 개념 설명 추가
- `README.md`: 구조 업데이트, 신규 파일 표시
- `BACKLOG-deferred.md`: 9~12번 항목 추가

## v1.0.0: 2026-06-17
- 최초 릴리스: agent-loop, spec-interview, guardrails, doc-sync, harness-design, kit
