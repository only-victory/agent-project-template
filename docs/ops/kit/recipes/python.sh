# --- 3) 이 프로젝트 검사: Python 자동화·스크립트 ---------------------------
# 교체 가이드: 패키지 매니저(uv/poetry/pip)에 맞춰라. 가상환경 전제.
# ruff·pytest가 없으면 해당 줄 삭제. mypy는 타입힌트 쓸 때만.

ruff check .                    # 린트 (flake8 쓰면 교체)
ruff format --check .           # 포맷 검사 (black 쓰면 교체)
# mypy .                        # 타입체크 (타입힌트 있으면 주석 해제)
pytest -q                       # 테스트 (테스트 없으면 삭제: 단 BACKLOG에 도입 신호 기록)
# --------------------------------------------------------------------------
